EDate stands for Extended Date, it is a wrapper for Date Javascript built-in object.

When EDate is created it returns a Date object with more function on its prototype. Those functions are similar to PHP.
